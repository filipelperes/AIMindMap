/**
 * Portuguese (Brazil) content for mind map nodes.
 * Start with English content — translate incrementally.
 *
 * To add a translation for a node, copy its structure from content.ts
 * and translate all string fields.
 */
import type { NodeContent } from '../types/mindmap'

export const nodeContentPtBR: Record<string, NodeContent> = {
  // Start with empty — translations will be added over time.
  // Example structure:
  // LLM: { ... fully translated Portuguese content ... }
}
